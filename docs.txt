Config ✅:
  1. Client Dockerized
  2. Nginx Config added
  3. Nginx.prod added
  4. Server Dockerized
  5. Docker DB installed(pg)
  6. Docker Compose for dev
  7. Docker Compose for prod
  8. Add prisma Config
  9. add Redis config

Prod: 
  1. fix OTP request limit to 3
  2. Rate LImit of 5 in login
  3. Redis Setex to 1-5 mins
  4. 


--------TEST_TODO VVII:--------
  2. check if one can place rental request on a already requested product.


TODO:
  4. Set diff price for ACADEMIC_BOOK category.
  5. verify@brittoo.xyz> fix this for other emails
  9. dashboard->mycredits->bccTransactions-> add a link/other-similar field here for rejected credits, on click of that reject reason will be shown
  10. Change JWT expiry 
  11. add rate limiter to others and redis cache.

PROBLEMS:
  1. After is_verified changed to verified from pending doesn't reflect until user logs out and again logs in.
  2. If more than 15 days selected during rental show message.

FEATURES_LATER:
  1. Users total earning, spent etc.

  Get SSL:
    1. Stop the Nginx at docker:
      $ sudo docker compose -f docker-compose-prod.yml down -v

    2. Install certbot in standalone mode
      $ sudo apt install certbot
      $ sudo certbot certonly --standalone -d brittoo.xyz -d www.brittoo.xyz

    3. After confirmation SSL certs at:
      $ /etc/letsencrypt/live/brittoo.xyz/

    4. Rebuild:
      $ sudo docker compose -f docker-compose.prod.yml up -d --build

    5. Auto-Renew SSL:
      $ sudo crontab -e
    
    6. Add this: 0 3 * * * certbot renew --pre-hook "docker compose -f /path/to/docker-compose.prod.yml down" --post-hook "docker compose -f /path/to/docker-compose.prod.yml up -d"
